import React from 'react';

interface Props {
    loading: boolean;
}

const HeroScreen = (props: Props) => {
    return (
        <>
            <p>Hero Screen</p>
        </>
    )
}

export default HeroScreen;